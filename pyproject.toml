[project]
name = "orchid_flow"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.135.1", "pydantic>=2.12.5"]

[dependency-groups]
dev = ["ipython>=9.10.0", "pytest>=8.0.0", "pdoc>=14.0.0"]
